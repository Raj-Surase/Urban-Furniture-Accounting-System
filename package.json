{
  "name": "odoo-hackathon-2026-starter",
  "version": "1.0.0",
  "description": "Full-stack hackathon starter skeleton with Laravel 13 API, React Vite, and Socket.io realtime",
  "scripts": {
    "setup": "bash setup.sh",
    "dev": "concurrently -k -n \"BACKEND,REALTIME,FRONTEND\" -c \"magenta.bold,yellow.bold,cyan.bold\" \"node scripts/dev-backend.js\" \"cd realtime && node server.js\" \"cd frontend && npm run dev -- --host\"",
    "dev:backend": "node scripts/dev-backend.js",
    "dev:realtime": "cd realtime && node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "test:realtime": "cd realtime && node test-client.js",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

